# Operations

Use `capabilities` for the complete live inventory. Commands cover environment/session, app/window/screen/UI/image observation, pointer/keyboard/clipboard/dialog/file-dialog/download interaction, task plan/preview/run/replay/recovery/cleanup, and owned process termination. Inputs are JSON strings passed with `--input`; output is one `desktop.v3` envelope.
