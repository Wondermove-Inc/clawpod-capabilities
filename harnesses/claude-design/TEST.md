# Test contract

Run `pytest -q harnesses/claude-design/tests/test_claude_design.py`.

Coverage includes stable envelopes; onboarding/auth redaction; undiscovered MCP boundary; exact preview/apply digests; project, design-system, template, code-sync, destination, and admin surfaces; exact-name deletion; identifier rejection; local export MIME/bytes/SHA-256; manifests and command-contract parity. No live credentials, connector installation, provider mutation, publication, or browser action occurs.
