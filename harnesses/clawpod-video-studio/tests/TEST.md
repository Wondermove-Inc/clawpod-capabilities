# Validation contract

Run `python -m unittest discover -s tests -v` from `harnesses/clawpod-video-studio`. Tests are offline, credential-free, and provider-free. Live provider or paid-media tests require separate owner approval and are intentionally excluded.
