# Test contract

Run `python3 -m pytest -q harnesses/youtube-evidence-analysis/tests/test_youtube_evidence_analysis.py`, the schema generator, repository validator, registry sync check, Python compilation, and `git diff --check`.
