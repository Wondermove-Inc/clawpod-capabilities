{
  "$schema": "../../schemas/package-metadata.schema.json",
  "schemaVersion": 1,
  "version": "0.1.0",
  "description": "Bounded, evidence-recorded read-only diagnostics for Linux hosts, networks, Kubernetes, and security hygiene, plus approval-bound plan/apply remediation for service restart, rollout restart, and managed-pod delete.",
  "compatibility": {
    "openclaw": ">=2026.4.0",
    "platforms": [
      "linux"
    ]
  },
  "safety": {
    "risk": "destructive",
    "approvalRequired": true
  }
}
