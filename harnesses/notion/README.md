# notion Harness

Stdlib-only typed wrapper for official Notion REST API 2026-03-11. Use `--preview` for every write, then pass its exact `intent_hash` through `--confirm`. Credentials are accepted only from protected runtime environment injection. See `../../docs/notion-contract.md`.
