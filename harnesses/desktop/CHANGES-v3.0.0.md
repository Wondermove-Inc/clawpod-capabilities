# Desktop 3.0.0

- Added trusted typed wrapper around the installed system `desktop` CLI.
- Added desktop.v3 envelopes, revision and idempotency state, approval previews, bounded deadlines, redaction, event records, and no-follow run roots.
- Added task/session recovery surfaces and explicit AT-SPI/D-Bus diagnostics.
- Added full command inventory and adversarial contract tests.
- Added runtime precision orchestration: accessibility-first targeting, explicit visual fallback, digest-bound coordinate context, bounded stale-target recovery, focus and click-postcondition verification, no replay of uncertain actions, and bounded linear drag paths.
- Added fail-closed portal session diagnostics and runtime precision adversarial tests while retaining the 67-command schema.
