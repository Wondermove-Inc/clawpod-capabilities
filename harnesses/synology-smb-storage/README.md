# Synology SMB Storage Harness

Run `./synology_smb_storage.py --help`. Requires Linux, `smbclient`, `mount.cifs`, mount privilege, and an approved protected password injection through `SYNOLOGY_SMB_PASSWORD` or stdin. It never stores credentials.
